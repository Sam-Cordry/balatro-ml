CREATE TABLE sessions (
    "session" int4 NOT NULL,
    "money" int4 NOT NULL,
    ante int4 NOT NULL,
    rounds_completed int4 NOT NULL,
    rounds_skipped int4 NOT NULL,
    hands int4 NOT NULL,
    discards int4 NOT NULL,
    hand_size int4 NOT NULL,
    last_consumable public.consumable NULL,
    high_card_level int4 NOT NULL,
    pair_level int4 NOT NULL,
    two_pair_level int4 NOT NULL,
    three_of_a_kind_level int4 NOT NULL,
    straight_level int4 NOT NULL,
    flush_level int4 NOT NULL,
    full_house_level int4 NOT NULL,
    four_of_a_kind_level int4 NOT NULL,
    straight_flush_level int4 NOT NULL,
    five_of_a_kind_level int4 NOT NULL,
    flush_house_level int4 NOT NULL,
    flush_five_level int4 NOT NULL,
    high_card_played int4 NOT NULL,
    pair_played int4 NOT NULL,
    two_pair_played int4 NOT NULL,
    three_of_a_kind_played int4 NOT NULL,
    straight_played int4 NOT NULL,
    flush_played int4 NOT NULL,
    full_house_played int4 NOT NULL,
    four_of_a_kind_played int4 NOT NULL,
    straight_flush_played int4 NOT NULL,
    five_of_a_kind_played int4 NOT NULL,
    flush_house_played int4 NOT NULL,
    flush_five_played int4 NOT NULL,
    pluto_used bool NOT NULL,
    mercury_used bool NOT NULL,
    uranus_used bool NOT NULL,
    venus_used bool NOT NULL,
    saturn_used bool NOT NULL,
    jupiter_used bool NOT NULL,
    earth_used bool NOT NULL,
    mars_used bool NOT NULL,
    neptune_used bool NOT NULL,
    planet_x_used bool NOT NULL,
    ceres_used bool NOT NULL,
    eris_used bool NOT NULL,
    overstock_redeemed bool NOT NULL,
    overstock_plus_redeemed bool NOT NULL,
    clearance_sale_redeemed bool NOT NULL,
    liquidation_redeemed bool NOT NULL,
    hone_redeemed bool NOT NULL,
    glow_up_redeemed bool NOT NULL,
    reroll_surplus_redeemed bool NOT NULL,
    reroll_glut_redeemed bool NOT NULL,
    crystal_ball_redeemed bool NOT NULL,
    omen_globe_redeemed bool NOT NULL,
    telescope_redeemed bool NOT NULL,
    observatory_redeemed bool NOT NULL,
    grabber_redeemed bool NOT NULL,
    nacho_tong_redeemed bool NOT NULL,
    wasteful_redeemed bool NOT NULL,
    recyclomancy_redeemed bool NOT NULL,
    tarot_merchant_redeemed bool NOT NULL,
    tarot_tycoon_redeemed bool NOT NULL,
    planet_merchant_redeemed bool NOT NULL,
    planet_tycoon_redeemed bool NOT NULL,
    seed_money_redeemed bool NOT NULL,
    money_tree_redeemed bool NOT NULL,
    blank_redeemed bool NOT NULL,
    antimatter_redeemed bool NOT NULL,
    magic_trick_redeemed bool NOT NULL,
    illusion_redeemed bool NOT NULL,
    hieroglyph_redeemed bool NOT NULL,
    petroglyph_redeemed bool NOT NULL,
    directors_cut_redeemed bool NOT NULL,
    retcon_redeemed bool NOT NULL,
    paint_brush_redeemed bool NOT NULL,
    palette_redeemed bool NOT NULL,
    CONSTRAINT sessions_check_pos CHECK (((money >= 0) AND (ante >= 0) AND (rounds_completed >= 0) AND (rounds_skipped >= 0) AND (hands >= 0) AND (discards >= 0) AND (hand_size >= 0) AND (high_card_level >= 0) AND (pair_level >= 0) AND (two_pair_level >= 0) AND (three_of_a_kind_level >= 0) AND (straight_level >= 0) AND (flush_level >= 0) AND (full_house_level >= 0) AND (four_of_a_kind_level >= 0) AND (straight_flush_level >= 0) AND (five_of_a_kind_level >= 0) AND (flush_house_level >= 0) AND (flush_five_level >= 0) AND (high_card_played >= 0) AND (pair_played >= 0) AND (two_pair_played >= 0) AND (three_of_a_kind_played >= 0) AND (full_house_played >= 0) AND (four_of_a_kind_played >= 0) AND (straight_flush_played >= 0) AND (five_of_a_kind_played >= 0) AND (flush_house_played >= 0) AND (flush_five_played >= 0))),
    CONSTRAINT sessions_pk PRIMARY KEY (session)
);
